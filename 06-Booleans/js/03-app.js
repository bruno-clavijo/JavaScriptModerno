const autenticado = false;

if(autenticado) {
    console.log("Si puede ver netflix");
} else {
    console.log("No pueder verlo");
}

//Operador ternario

console.log(autenticado ? 'Si esta autenticado' : 'No esta autenticado');